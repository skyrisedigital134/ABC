// Initialize AOS (Animate On Scroll)
AOS.init({
    duration: 1000,
    once: false,
    mirror: true
});

// Smooth scroll logic or 3D cursor effects can be added here
document.addEventListener('mousemove', (e) => {
    // Optional: Add a custom gold cursor or light-follow effect
});

console.log("SkyRise Digital Experience Loaded.");
